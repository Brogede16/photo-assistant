services:
  - type: web
    name: photo-assistant
    runtime: static
    branch: main
    autoDeploy: true
    buildCommand: rm -rf dist && mkdir -p dist && cp -R index.html service-worker.js src public dist/
    staticPublishPath: ./dist
